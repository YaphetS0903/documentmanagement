# 文档管理平台正式版发布检查清单

目标版本：`1.0.0-rc.1` -> `1.0.0`

## 一、发布候选版

- [x] 四期 M1-M5 功能完成并形成验收报告。
- [x] DOCX、XLSX、PPTX 在远程真实环境完成编辑和新版本保存。
- [x] API 冒烟测试、生产构建、语法和 Git 差异检查通过。
- [x] 文件迁移工具完成扫描、演练、执行、分片上传和断点续跑验证。
- [x] 中文文件名在文档名称、版本原名、存储名称和下载内容中保持正确。
- [x] 小文件与 9MB 分片文件完成大小、MD5、下载内容和上传审计对账。
- [x] 仓库不包含数据库密码、JWT、SSH 文件、运行数据和备份。
- [x] 生产环境禁止使用开发 JWT，登录页不展示或预填默认账号密码。
- [x] ONLYOFFICE 禁用插件自动更新并设置内存保护上限。

### 自动验收记录（2026-07-15）

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| 迁移工具单元测试 | `npm run migration:test` | 通过 |
| API 全流程冒烟测试 | `npm test` | 通过，且连续执行无端口残留 |
| 前端生产构建 | `npm run build` | 通过，存在非阻断的大包体积提醒 |
| 发布检查 | `npm run release:check` | 通过，版本 `1.0.0-rc.1` |
| 生产依赖审计 | `npm audit --omit=dev` | 通过，0 个已知漏洞 |
| Git 差异格式 | `git diff --check` | 通过 |
| 敏感信息扫描 | 私钥、数据库 URL、GitHub Token、JWT 模式扫描 | 未发现真实凭据 |
| 隔离迁移 | 2 个文件、4 个目录、约 9MB | 通过，重复执行恢复 2 个文件且未重复上传 |

本节只代表代码和隔离环境验收通过，不替代正式服务器、正式 MySQL、真实账号和真实业务文档验收。

## 二、生产环境

- [ ] 正式 MySQL 连接成功，运行状态不得降级为 JSON。
- [ ] 修改管理员及演示账号默认密码，停用不需要的演示账号。
- [ ] 配置正式域名、HTTPS 证书、反向代理和可信访问范围。
- [ ] 配置随机 `JWT_SECRET`、ONLYOFFICE JWT 和企业微信凭据。
- [ ] 确认 `DATA_DIR`、`UPLOAD_DIR`、`TMP_DIR`、`BACKUP_DIR`、`QUARANTINE_DIR` 位于持久化磁盘并具备正确权限。
- [ ] 执行 `npm run production:check` 并通过。
- [ ] 验证平台、MySQL、ONLYOFFICE、磁盘和同步目录健康状态。

## 三、用户验收

- [ ] 管理员完成中文文件上传、下载、预览、在线编辑、版本、权限和删除恢复流程。
- [ ] 普通员工完成可见范围、同步目录、未读提醒、收藏、订阅、借阅和审批流程。
- [ ] 使用 DOCX、XLSX、PPTX、PDF、图片、Markdown、Python 和大文件各至少一个进行验收。
- [ ] 验证管理员可配置同步目录，普通员工只能执行同步且不能修改目录范围。
- [ ] 企业微信真实账号完成成功投递、失败记录和人工重试。
- [ ] 桌面和手机视口无严重遮挡、横向溢出或控制台错误。

## 四、数据迁移验收

- [ ] 业务负责人确认旧目录到新目录的映射、权限矩阵和同名文件处理方式。
- [ ] 选取不超过正式数据 5% 的代表性目录完成扫描、演练和试迁移。
- [ ] 扫描文件数等于迁移成功数、跳过数与失败数之和。
- [ ] 按一级目录和主要扩展名抽查至少 50 个文件，核对名称、大小、内容与权限。
- [ ] 对抽样文件比对源文件与平台下载文件的 MD5。
- [ ] 验证迁移中断后使用同一断点文件续跑，不产生重复文件或重复版本。
- [ ] 试迁移通过后安排旧系统只读窗口、全量迁移和最后一次增量补迁。

## 五、发布与回滚

- [ ] 发布前创建完整备份并通过恢复演练。
- [ ] 记录当前数据库、上传目录、配置和应用版本。
- [ ] 将候选版升级为 `1.0.0`，执行测试、构建并创建 Git 标签。
- [ ] 发布后完成健康检查和核心业务冒烟测试。
- [ ] 若出现阻断问题，恢复上一应用版本及发布前数据备份。

## 六、正式发布判定

以下条件全部满足后，才允许从 `1.0.0-rc.1` 升级为 `1.0.0`：

- [ ] 生产环境、用户验收、数据迁移验收和备份恢复演练全部完成。
- [ ] 没有 P0/P1 缺陷，剩余问题已明确责任人和处理日期。
- [ ] 业务负责人、系统管理员和项目负责人确认上线窗口与回滚条件。
- [ ] 发布后至少观察一个完整工作日，核心接口、错误率、磁盘和内存无异常增长。
